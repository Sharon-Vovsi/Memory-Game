/*open the board*/
function playGame() {
    const url = '/index';
    window.open(url);
}

/*open login*/
function openLogin() {
    const url = '/login';
    window.open(url);
}

/*open sign up*/
function register() {
    const url = '/register';
    window.open(url);
}
/*--------------------------------------------------*/

